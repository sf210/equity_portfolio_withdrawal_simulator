# Third-party dependencies. Everything else used by these scripts is in the
# Python standard library. Developed/tested against Python 3.14 and the version
# pinned below. To allow newer NumPy 2.x, relax this to: numpy>=2,<3
numpy==2.4.4
